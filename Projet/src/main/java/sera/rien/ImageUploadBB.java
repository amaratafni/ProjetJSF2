package sera.rien;
/*
 * package com.beans;
 * 
 * import java.io.File; import java.io.FileOutputStream; import
 * java.io.InputStream;
 * 
 * import org.primefaces.model.UploadedFile;
 * 
 * public class ImageUploadBB {
 * 
 * private UploadedFile uploadedFile;
 * 
 * public UploadedFile getUploadedFile() { return this.uploadedFile; }
 * 
 * public void setUploadedFile( UploadedFile uploadedFile ) { this.uploadedFile
 * = uploadedFile; }
 * 
 * // Méthode associée au bouton Uploader public String actionUpload(){
 * 
 * InputStream is = uploadedFile.getInputstream();
 * 
 * byte[] buffer = new byte[uploadedFile.);
 * 
 * is.read(buffer); File f = new File("cheminLocal" +
 * uploadedFile.getFileName()); f.createNewFile(); FileOutputStream fos = new
 * FileOutputStream(f); fos.write(buffer); //Enregistrement de l'URL de
 * uploadedFile.getFilename() dans la BDD } }
 */